export const metadata = {
  title: "PEMM Mission",
  description: "PEMM Mission",
};

export default function MissionLayout({ children }) {
  return (
    <main>
      <div>{children}</div>
    </main>
  );
}
