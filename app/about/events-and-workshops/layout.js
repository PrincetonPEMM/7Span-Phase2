export const metadata = {
  title: "PEMM Events",
  description: "PEMM Events",
};

export default function EventsAndWorkshopsLayout({ children }) {
  return (
    <main>
      <div>{children}</div>
    </main>
  );
}
