export const metadata = {
  title: "PEMM Team",
  description: "PEMM Team",
};

export default function PeopleLayout({ children }) {
  return (
    <main>
      <div>{children}</div>
    </main>
  );
}
