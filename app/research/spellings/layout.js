export const metadata = {
  title: "PEMM Spellings",
  description: "PEMM Spellings",
};

export default function SpellingsLayout({ children }) {
  return (
    <main>
      <div>{children}</div>
    </main>
  );
}
