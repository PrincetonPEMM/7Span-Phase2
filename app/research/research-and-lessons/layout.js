export const metadata = {
  title: "PEMM Research",
  description: "PEMM Research",
};

export default function ResearchAndLessonsLayout({ children }) {
  return (
    <main>
      <div>{children}</div>
    </main>
  );
}
