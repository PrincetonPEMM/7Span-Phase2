export const metadata = {
  title: "PEMM Macomber Handlist",
  description: "PEMM Macomber Handlist",
};

export default function MacomberLayout({ children }) {
  return (
    <main>
      <div>{children}</div>
    </main>
  );
}
