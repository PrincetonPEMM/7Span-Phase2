export const metadata = {
  title: "PEMM Maps",
  description: "PEMM Maps",
};

export default function MapsLayout({ children }) {
  return (
    <main>
      <div>{children}</div>
    </main>
  );
}
