export const metadata = {
  title: "PEMM Bibliography",
  description: "PEMM Bibliography",
};

export default function BibliographyLayout({ children }) {
  return (
    <main>
      <div>{children}</div>
    </main>
  );
}
