export const metadata = {
  title: "PEMM Repositories",
  description: "PEMM Repositories",
};

export default function RepositoriesLayout({ children }) {
  return (
    <main>
      <div>{children}</div>
    </main>
  );
}
