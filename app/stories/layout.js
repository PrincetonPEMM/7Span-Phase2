export const metadata = {
  title: "Stories",
  description: "Generated by Next.js",
};

export default function StoriesLayout({ children }) {
  return (
    <main>
      <div>{children}</div>
    </main>
  );
}
