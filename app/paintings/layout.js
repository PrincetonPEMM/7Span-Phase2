export const metadata = {
  title: "PEMM Paintings",
  description: "PEMM Paintings",
};

export default function PaintingsLayout({ children }) {
  return (
    <main>
      <div>{children}</div>
    </main>
  );
}
